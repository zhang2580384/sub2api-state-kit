# 插件版验证记录

对象：STATE Kit 独立插件 `4.2.1`，宿主基线为官方 Sub2API `v0.2.7`。

## 自动化验证

2026-09-22 执行：

- `go test ./... -count=1`：通过。
- `go vet ./...`：通过。
- `node --test ui-tests/*.test.cjs`：30 项通过。
- `node --check ui/assets/app.js`：通过。
- `node --check ui/assets/bridge-v1.js`：通过。
- Python 打包脚本语法检查：通过。
- Linux amd64、Linux arm64、macOS arm64 交叉编译：通过。
- Linux amd64 包验签通过；payload `7` 个，runtime platform 为 `linux-amd64`。
- Linux arm64 包验签通过；payload `7` 个，runtime platform 为 `linux-arm64`。
- 全平台包验签通过；payload `9` 个，runtime platforms 为 `linux-amd64`、`linux-arm64`、`darwin-arm64`。
- 当前本地环境没有 CGO，因此本轮未执行 `go test -race`；普通 Go 测试和 `go vet` 已通过。

测试覆盖：

- 旧配置缺省兼容和 strict JSON / 未知字段拒绝；
- 旧模式同出口绑定；
- 旧模式请求头透传，包括调用方 Cookie 和 `session_id`；
- Cookie 模式的独立采集、业务出口和 Cookie 注入；
- Cookie 模式要求 STATE、`session_id` 和至少一个响应 Cookie；
- Cookie 响应回写、`session_id` 保持和敏感代理凭据不落盘；
- 备用票队列、提前量、晋级和“新票验证前不替换当前票”；
- 旧模式备用票复用当前生成出口，准备期间不替换仍有效的主票；
- 备用票晋级前的业务出口指纹和账号身份指纹检查；
- 上一轮生成器出口复用、地区过滤、持久化恢复和异常守护；
- UI 模式切换、字段校验、保存、状态显示和诊断脱敏。

### 4.2.1 获取链路优化

- 生成出口的 IP 与地区合并为一次查询；
- 同出口复验只复核 IP 是否变化，不重复查询地区；
- 地区过滤、312、STATE 长度不符和复验失败使用快速重试；
- 旧模式可以提前准备备用票，优先复用当前生效的生成出口，复用失败后自动换新出口；
- 采集诊断耗时包含代理生成、出口检查和打票探测。

这些改动保留了“同出口复验通过后才可用”的安全边界，没有把未经复验的 STATE 直接投入业务。

## 真实代理探测

### 上游 292 生命周期

修正后的跟踪器记录到 6 个明确结束样本：

| 同一 292 持续 | 结束信号 |
| --- | --- |
| `6.05` 分钟 | `312 + gpt-5.6-luna` |
| `5.15` 分钟 | `312 + gpt-5.6-luna` |
| `5.00` 分钟 | `312 + gpt-5.6-luna` |
| `4.11` 分钟 | `312 + gpt-5.6-luna` |
| `1.13` 分钟 | `312 + gpt-5.6-luna` |
| `1.13` 分钟 | `312 + gpt-5.6-luna` |

这些样本的代理出口未变化，说明结束点是上游 292 会话变化，不是代理掉线或换 IP。`10` 分钟可能成功，但不能作为稳定下限。因此 Cookie 模式默认持续期设为 `300` 秒，备用票默认提前 `90` 秒。

### 粘性出口

使用：

```text
sessType=sticky&sessTime=180
```

`sessAuto=1` 的一次样本在约第 `18.74` 分钟更换出口，不能用于长期固定出口。改为 `sessAuto=0` 后，同一生成端口的出口已连续保持 `87.91` 分钟未变；尚未完整跑到 180 分钟。

### 旧模式：无 Cookie 跨出口

无完整 Cookie 时进行的跨出口对照试验为 `3/3` 次失败：

| 请求阶段 | 出口 | 结果 |
| --- | --- | --- |
| 采集 292 | 生成粘性出口 A | `292 + gpt-6-astra` |
| 使用同一 STATE | 第一层固定代理 | `312 + gpt-5.6-luna` |

切回原生成出口后恢复 `gpt-6-astra`。这说明旧模式不能只在打票阶段使用粘性出口，业务请求也必须保持同一出口。

### Cookie 分流：跨出口成功

保持完整 Cookie 和同一个 `session_id`，打票与业务使用不同出口的方案曾连续成功 `4/4`，业务响应继续返回目标模型。

结论：

- 打票响应必须提供 Cookie；只有 STATE 或只有 `session_id` 不算完整 Cookie 票据。
- 打票、复验和业务请求必须继续使用同一个 `session_id`，响应中更新的 Cookie 会合并回票据。
- 业务出口和采集出口不再强制相同，但香港 `HK` 和未知地区仍会被拒绝。
- 样本仍有限，不能把 `4/4` 当作所有账号、地区和上游状态下的永久保证。

## 精准快速出票

当前实现不是本地生成 292，无法保证每次快速、准确地得到 292。已实现的控制方式是：

- 提前准备备用票，而不是等当前票失效后同步获取；
- 当前票仍可用时继续服务，新票复验成功前不替换；
- 备用票晋级前检查账号身份、模型、业务出口、STATE、`session_id` 和 Cookie；
- 生成器模式可优先复用当前账号上一轮通过复验的出口；
- 可用“备用票提前量”平衡等待时间和代理调用频率。

尚不能保证：

- 生成器一次调用一定返回可用 292；
- 新票据一定在备用票提前量内完成；
- 单张 292 固定持续 5 分钟或更久；
- 所有出口、地区和账号具有相同成功率。

下一步若继续优化，应以真实诊断事件统计“生成器耗时、采集耗时、复验耗时、292 成功率、备用票命中率”，再根据 P50 / P95 调整提前量，而不是继续增加固定猜测值。

## 当前安装包

源码包和全部附件的最终摘要以 Release 的 `SHA256SUMS` 为准。发布私钥未进入仓库、源码包或插件包。

## 未完成项

- 尚未把 `4.2.1` 部署回线上 Sub2API 实例执行升级和回退验收。
- 粘性出口长测已观察到 `87.91` 分钟未换 IP，但尚未完整运行到 180 分钟。
- Cookie 分流跨出口已有连续 `4/4` 实测，仍需更多账号、地区和长时间样本。
- 单张 292 尚未观察到持续 10 分钟以上的本轮样本，因此不宣称上游 292 能保持 180 分钟。
- 不承诺所有生成出口都能取得 292，也不承诺消除 429、配额或上游路由变化。
